{
  "name": "parraski-photos",
  "version": "1.0.0",
  "private": true,
  "description": "Photography portfolio for parraski.com — static site, built from originals/ into public/.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "rebuild": "node scripts/build.mjs --force",
    "serve": "node scripts/serve.mjs",
    "dev": "node scripts/build.mjs && node scripts/serve.mjs"
  },
  "dependencies": {
    "exif-reader": "^2.0.2",
    "sharp": "^0.35.4"
  },
  "engines": {
    "node": ">=20"
  }
}
